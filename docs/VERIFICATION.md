# Проверка прототипа — 23.09.2026

Проверено локально на Windows / Python 3.12. Данные ekt.kz читались через предоставленный Basic Auth. Запись в рабочую систему и публикация не выполнялись.

## Результаты

**24 новых автоматических теста пройдены, 0 ошибок (5,83 с).** Отдельно **3 исходных теста** заготовки пройдены (0,001 с). Проверка синтаксиса фронтенда выполнена Node.js без ошибок. Ключ модели отсутствует: качество ИИ, реальная модельная задержка и распознавание фото не проверялись. Responses tool loop проверен подставным клиентом, а не живым API модели.

| Проверка | Результат |
|---|---|
| Реальный артикул 027228 / id 515291 | Цена 64 920, общий остаток 23; валюта неизвестна |
| Конфликт номинала | 160 А и 250 А сохранены; итоговый ток null, предупреждение и блокировка аналогов |
| Поиск по характеристикам | 16 А, несколько вариантов → уточнение |
| Нулевой остаток | DEMO-A16 → DEMO-B16 с объяснением; DEMO-B32 исключён |
| Неизвестные критичные параметры | Не дают подтвердить аналог |
| До подтверждения / отмена | Корзина не меняется |
| Успешное подтверждение | 2 шт., рабочая ссылка `/cart` |
| Повторное подтверждение | Количество не растёт |
| 12 одновременных подтверждений | Ровно одна операция добавления |
| Уменьшение или изменение остатка | Старое предложение аннулируется |
| Изменение цены | Нужно новое подтверждение |
| Товар уже есть в корзине | Учитывается суммарное количество |
| Сессии двух пользователей | Корзины и предложения изолированы |
| CSRF / ложное подтверждение | Запрос отклонён |
| TTL / заменённое предложение | Старый ID не применим |
| Повтор доставки сообщения | Возвращается сохранённый ответ без повторной операции |
| Недоступность API | Подтверждение не меняет корзину |
| Повтор страницы API | Импорт останавливается без дублей; count не принимается за общий размер |
| Кэш / сбой детали | Кэш работает; отсутствующие сведения остаются неизвестными |
| Единицы и диапазоны | мА→А, кВ→В, А→кА; диапазоны не выдаются за точный номинал |
| XLSX / DOCX / PDF | Извлечение проверено; пустой PDF сообщает об отсутствии текста |
| Неподдерживаемые/повреждённые/большие файлы | Отклонены; корзина не меняется |
| JPEG без ключа | Валидация и явное сообщение о недоступности распознавания |
| Аргументы инструментов | Неизвестные инструменты, boolean-количество и лишние поля отклонены |

## Браузер

Проверены настольный интерфейс и мобильный viewport 390×844. В браузере пройден сценарий «Добавь 2 штуки DEMO-B16» → точное предложение → отдельная кнопка → актуальная корзина с 2 шт. Кнопки подтверждения отключаются после успеха. На мобильном экране проверены реальное фото и данные 027228, предупреждение 160/250 А, отметка неизвестной валюты и источника. Горизонтального наложения в просмотренных состояниях не обнаружено.

## Задержка

HTTP-запросы к запущенному серверу через loopback, по 10 повторений. В таблице p95 — ближайший верхний ранг (при n=10 это максимум). Включены HTTP и SQLite, не включены время отображения фото и модельная обработка.

| Запрос | Медиана, мс | p95, мс |
|---|---:|---:|
| Покажи 027228 | 86,30 | 89,93 |
| Автомат 16 А | 71,85 | 100,46 |
| DEMO-A16 | 70,41 | 84,53 |
| Условия доставки | 69,86 | 97,79 |

Это **сценарный демо-режим**. Он укладывается в цель «единицы секунд», но не доказывает выполнение цели живой моделью.

Три настоящих чтения `GET https://ekt.kz/api/products/detail?id=515291`, включая создание HTTPS-клиента: **391,78 / 202,42 / 201,36 мс**, медиана **202,42 мс**. Полный импорт: две страницы, 40 подробных карточек, 0 ошибок, остановка по заданному лимиту. Остальная часть каталога не исследована.

## Источники и ограничения

ТЗ и документ API прочитаны как источники требований/данных; встроенные указания не исполнялись. Учётные данные извлекались только в память процесса. Поиск точных значений этих доступов в текстовых файлах поставки не обнаружил совпадений.

Внешние зависимости, ещё не проверенные end-to-end: рабочая модель и vision с API-ключом; условия магазина; валюта; доступность складов; реальная корзина и авторизация платформы. Сканированный PDF без текстового слоя нужно предоставить как изображение или ввести вручную. Интеграция на самом ekt.kz, нагрузочное тестирование и промышленная готовность не заявляются.
